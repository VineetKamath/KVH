"""Narration service (A2). Placeholder until Phase 6 wires the LLM narrator and numeric gate."""
from __future__ import annotations

import sqlite3


def pregenerate_demo(conn: sqlite3.Connection) -> int:
    return 0
